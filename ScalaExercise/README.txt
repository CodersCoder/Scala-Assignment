To run different problem statements:

1. AadhaarScala code :
   Pass the arguments as 
   	1. input file path of auth file
   	2. output folder path

1. AadhaarSpark code :
   Pass the arguments as 
   	1. input file path of auth file
   	2. output folder path

1. PovertyReport code :
   Pass the arguments as 
   	1. input file path of Poverty Estimates excel file
   	2. input file path of States name excel file
   	3. output folder path
 
 To build jar of the project use Maven. Added all the necessary dependencies in the pom.xml.
 
 In the states file, the data was not correct. So I downloaded the data from wikipedia and used it to code.
